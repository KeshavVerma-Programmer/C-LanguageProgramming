#include<stdio.h>
int main()
{
    float x,y,z,det,root1,root2,real,img;
    printf("enter the value of x,y,z:");
    scanf("%f,%f,%f",&x,&y,&z);
    det=y*y-4*x*2;
    if(det>0)
    {
        root1=(-y+sqrt(det))/(2*x);
        root2=(-y+sqrt(det))/(2*x);
        printf("\n value of root=%2f,root+20f=%2f,root1,root2");
        if (det==0)
        {
            y(2*x)
            else}
    }

    printf("\n value of root=%2f+%ff and value,real,img");

    return 0;
}
